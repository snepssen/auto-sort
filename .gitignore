__pycache__/
*.py[cod]
.DS_Store
build/
dist/
*.egg-info/
# Written by the daemon, once there is one. Never committed: it holds the
# ledger of every file this machine has moved.
state.db
rules.ini
# Backups of it: the same rules, learnt from one person's files.
rules.ini.*
